package:
  name: idb
  publish:
    version: 0.1.0
    license: Apache-2.0
    location:
      githubOwner: philippedev101
      githubRepo: purescript-idb
  description: PureScript FFI bindings for IndexedDB via the idb library
  dependencies:
    - aff
    - effect
    - js-promise
    - js-promise-aff
    - maybe
    - nullable
    - prelude
    - foreign
    - arrays
  test:
    main: Test.Main
    dependencies:
      - console
      - assert
workspace:
  extraPackages: {}
  packageSet:
    registry: 63.3.0
