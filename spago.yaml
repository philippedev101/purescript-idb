package:
  name: idb
  description: PureScript FFI bindings for IndexedDB via the idb library
  license: Apache-2.0
  repository: https://github.com/philippedev101/purescript-idb
  dependencies:
    - aff
    - effect
    - js-promise
    - js-promise-aff
    - maybe
    - nullable
    - prelude
    - foreign
    - arrays
  test:
    main: Test.Main
    dependencies:
      - console
      - assert
workspace:
  extraPackages: {}
  packageSet:
    registry: 63.3.0
